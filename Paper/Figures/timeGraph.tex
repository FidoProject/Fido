\footnotesize
\begin{tikzpicture}[font=\sffamily]
\begin{axis}[
    	symbolic x coords={flash an LED,
    					   drive to emitter,
    					   float to emitter,
    					   drive in a shape,
    					   line following},
    	ylabel={time (seconds)},
    	width = 14cm, height = 7cm,
        grid=major,grid style={dashed, gray!30},
        ymin=0,xtick=data]
    
    \addplot[ybar,pattern=north west lines] coordinates {
        (flash an LED,1.7)
        (drive to emitter,3)
        (float to emitter,2.8)
        (drive in a shape,2.4)
        (line following, 2.0)
    };
\end{axis}
\end{tikzpicture}